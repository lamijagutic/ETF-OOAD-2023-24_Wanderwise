@model IEnumerable<wdws.Models.Recenzija>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.tekstRecenzije)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.putovanje)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.tekstRecenzije)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.putovanje.ID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RecenzijaID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RecenzijaID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RecenzijaID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
