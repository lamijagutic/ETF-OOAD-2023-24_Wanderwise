@model IEnumerable<wdws.Models.Pasos>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.clientID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.drzavaKojaIzdaje)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.nacionalnost)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.datumIsteka)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.napomene)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.clientID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.drzavaKojaIzdaje)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.nacionalnost)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.datumIsteka)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.napomene)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
